# オフィス画面の作り込み仕様 ／ 机の島とスローガン

2026-09-01、内田さんの指示を受けて、こと（Claude Code）が仕様を確定させたもの。実装はおと（Codex）が担当する。

> 電話を取るまでの画面が殺風景。普通のオフィスに電話機とPCがあって、それが複数の机が並んでいて、オフィス部屋の壁にはスローガンがあって、それは起動するたびにランダムに変わって欲しい。

**この文書が仕様の正本。** `SPEC_call_flow_v2.md`（通話フロー）とは独立していて、触る箇所も重ならない。

---

## 1. いまの何が殺風景か

`office-floor` に什器が6つ、2列グリッドで並んでいるだけになっている。

- **机が1つしかない**（自席のみ）。オフィスに見えず、カードが並んだ画面に見える
- **壁がない**。部屋の奥行きも境界もない
- 旧版（PC向け3ペイン時代）には「空席A〜D」と夜景の窓があったが、スマホ専用化のときに削除された。**あれを、より部屋らしい形で戻す**

深夜のフロアに一人だけ座っている、という設定が絵で伝わるようにしたい。

---

## 2. 作るもの

### 2-1. 部屋の構造（画面の上から下へ）

```
┌─────────────────────────────┐
│  壁：今月のスローガン（全幅）      │ ← 新規
├─────────────────────────────┤
│  全社ホワイトボード（全幅）        │ ← 既存
├─────────────────────────────┤
│  あなたの自席（全幅・押せる）      │ ← 既存＋PCを追加
│    PC（点灯）＋ 電話機（鳴る）     │
├──────────────┬──────────────┤
│  空席（PC消灯）  │  空席（PC消灯） │ ← 新規
├──────────────┼──────────────┤
│  空席（PC消灯）  │  空席（PC消灯） │ ← 新規
├──────────────┼──────────────┤
│  SV席          │  時計          │ ← 既存
├──────────────┼──────────────┤
│  配送カウンター  │  書類トレイ     │ ← 既存
└─────────────────────────────┘
```

**空席は4つ。** 2×2で島になっているように見せる。5つ以上だと縦に伸びすぎて、スマホでスクロールが増える。

### 2-2. 机の上のもの

各机に **PC（モニタ）と電話機**を置く。

| | PC | 電話機 |
| --- | --- | --- |
| 自席 | **点灯**（画面が薄く光る） | 既存の `.phone-icon`。着信時に鳴る演出 |
| 空席 ×4 | **消灯**（暗いグレーの画面） | 小さく静かに置いてある |

**自席のPCが光り、まわりの4台が消えている**——これだけで「深夜に一人」が伝わる。文字で説明しない。

---

## 3. スローガン

### 3-1. データ（`p2_data.js`）

```js
/* 壁に貼られた今月のスローガン。シフトを始めるたびに1つ選ばれる */
const SLOGANS = [
  '凡事徹底',
  'クイックレスポンス',
  '顧客目線',
  'たゆまぬベンチャーマインド',
  '上場まで1000日',
  'ボーリングのセンターピンを抑えろ',
];
```

**文言は内田さんの指示のまま。1文字も変えないこと。** 「ボーリング」「抑えろ」の表記も指示どおりで、こちらの判断で「ボウリング」「押さえろ」に直さない。社内標語は固有の文言で、直すかどうかは内田さんが決めることになる。

### 3-2. いつ選ぶか

**`resetGame()` で1つ選び、`state.slogan` に持つ。** 「もう一度シフトに入る」でも選び直される。

```js
state.slogan = SLOGANS[Math.floor(Math.random() * SLOGANS.length)];
```

`renderOffice()` で `$('office-slogan').textContent = state.slogan;` を書き込む。**通話画面へ移ってオフィスに戻っても変わらない**（1シフト＝1スローガン）。

### 3-3. 見せ方

社内の掲示物らしく作る。額縁ではなく、**壁に直接貼られた掲示**。

- 上に小さく「今月のスローガン」というラベル（`.eyebrow` と同じ扱い）
- その下に大きく標語
- 少しだけ傾ける（`transform: rotate(-0.4deg)`）と、貼り紙らしくなる。**傾けすぎない**
- フォントは `--display`、太字、字間をやや広く
- 「ボーリングのセンターピンを抑えろ」が最長（16文字）。**スマホ幅で2行に折り返しても崩れないこと**を確認する

---

## 4. 描き方の制約（設計の芯を壊さないこと）

HANDOVER.md の「絶対に壊してはいけない設計の芯」に触れる。

- **⑦ 人物・キャラクターを描かない。** 机・PC・電話・椅子までが什器で、人は描かない。空席に人影を置かない
- **⑧ 外部依存を増やさない。** 画像ファイル・アイコンフォント・CDNは使わない。**CSSの矩形と擬似要素だけで描く**（いまの `.phone-icon` と同じ作り方）
- **② ダークに戻さない。** ライト業務画面のトークン（`--surface` / `--line` / `--accent` など）を使う。PCの消灯画面だけは暗い色でよい（液晶 `.lcd` が `#10212B` を使っているのと同じ扱い）
- **スマホ幅（最大480px）で成立させる。** 空席は `min-height` を小さめにして、縦に伸びすぎないようにする

---

## 5. DOM の形

`office-floor` の中に追加する。**既存の什器の順番と id は変えない**（`renderOffice()` が参照している）。

```html
<div class="office-floor">

  <!-- 新規：壁のスローガン -->
  <div class="office-wall">
    <span class="slogan-label">今月のスローガン</span>
    <b class="slogan-text" id="office-slogan"></b>
  </div>

  <!-- 既存 -->
  <div class="office-object office-board">…</div>

  <!-- 既存＋PCを追加 -->
  <button class="office-object office-desk" id="office-phone" data-office-phone="1" aria-label="自席の電話。着信一覧を開く">
    <b>あなたの自席</b>
    <small id="office-phone-status">待機中</small>
    <i class="desk-pc on" aria-hidden="true"></i>
    <i class="phone-icon" aria-hidden="true"></i>
  </button>

  <!-- 新規：空席 ×4 -->
  <div class="office-object office-seat" aria-hidden="true">
    <b>空席</b><small>無人</small>
    <i class="desk-pc" aria-hidden="true"></i>
    <i class="desk-phone" aria-hidden="true"></i>
  </div>
  <!-- 同じものを4つ -->

  <!-- 以下すべて既存のまま -->
  <div class="office-object office-sv" id="office-sv">…</div>
  <div class="office-object office-ship">…</div>
  <div class="office-object office-tray">…</div>
  <div class="office-object office-clock" id="office-clock">22:00</div>
  <aside class="office-incoming" id="office-incoming" hidden>…</aside>
</div>
```

`.office-wall`・`.office-board`・`.office-desk` は `grid-column:1 / -1`（全幅）。`.office-seat` は1列ぶんで、4つ並べると2×2になる。

**`.office-seat` には `aria-hidden="true"`** を付ける。読み上げに「空席 無人」が4回続いても情報にならないため。

---

## 6. CSSの方針

新しく描くのは3つだけ。

**`.desk-pc`（モニタ）** — 小さい矩形＋台座。`::before` で首、`::after` で足を出す。消灯時は暗いグレー（`#1A2A35` 程度）、`.on` のときは画面が薄く光る（`--accent-soft` に内側の光）。

**`.desk-phone`（空席の電話機）** — 既存の `.phone-icon` を小型化した形。本体の矩形＋`::before` で受話器。色は `--line`（静かに置いてある感じ）。**鳴る演出は付けない**。

**`.office-wall`（壁の掲示）** — `--surface` の面に薄い枠、わずかに傾ける。ラベルと標語の2段組み。

既存の `.phone-icon` と `.office-desk.ringing` の演出は**触らない**。あそこは「電話が鳴る」というゲームの合図なので、見た目を変えると意味が薄れる。

---

## 7. 検査

### 7-1. `check_invariants.js` に足す独立期待値

- `SLOGANS` が**ちょうど6件**で、**§3-1 の6つと文言が完全一致**する（順番も含めて）
- どの文言も空文字でない

### 7-2. `ui_contract_test.js` に足す契約

- `p1_head.html` に `id="office-slogan"` の要素がある
- `.office-seat` が**4つ**ある
- `.desk-pc` が**5つ**ある（自席1＋空席4）。うち自席のものだけ `on` が付いている
- `p3_game.js` の `resetGame()` が `state.slogan` を設定している
- 画像の外部参照（`<img`・`url(http`）が**増えていない**（設計の芯⑧）

### 7-3. negative test（壊したら必ず落ちること）

**「全通過」は合格の根拠にならない。** 実装後、次を1つずつ外して、担当の検査が赤くなることを確認すること。

1. `SLOGANS` から1件削る → `check_invariants` が落ちる
2. 「ボーリングのセンターピンを抑えろ」を「ボウリングのセンターピンを押さえろ」に直す → `check_invariants` が落ちる
3. 空席を3つに減らす → `ui_contract_test` が落ちる
4. `resetGame()` から `state.slogan` の設定を消す → `ui_contract_test` が落ちる
5. 自席の `.desk-pc` から `on` を外す → `ui_contract_test` が落ちる
6. スローガンを画像に置き換える（`<img>` を足す） → `ui_contract_test` が落ちる

**検査スクリプトは `__dirname` 基準のまま維持すること。**

---

## 7-B. ファミコン風ドット絵への作り替え（2026-09-01 内田さん指示）

> オフィスの絵はこんな感じ。これをドット絵にしてファミコン風にして欲しい

内田さんが実際のオフィス写真を見せたうえでの指示。**いまのカード的な表現をやめ、フロアを1枚のドット絵として描く。**

### 7-B-1. 写真から取り込む要素

内田さんが見せた写真にあるもの。**これらが絵に入っていれば「あのオフィス」に見える。**

- **天井の蛍光灯が格子状に規則的に並ぶ**（この写真でいちばん目を引く要素）
- **島型デスク**。長机が2列向かい合わせで、間に低いパーティション
- 各席に**モニタとビジネスフォン**
- 机の下に**袖机（引き出し3段）**
- **黒〜濃紺のキャスター椅子**
- **青灰色のカーペット**
- 奥に**窓とブラインド**、白いキャビネット、書類の山

### 7-B-2. 絵づくりはおとに委ねる（2026-09-01 内田さん指示）

> ドット絵はChatGPTの方が得意だから音に依頼した方がいいね（内田さん）

**パレット・タイル設計・レイアウト・描画方法は、おとの裁量で決めてよい。** この文書が指定するのは「何が描かれているべきか」（7-B-1）と「守るべき制約」（7-B-3）だけで、**どう描くかは縛らない。**

以下はこちらで試作したときの知見。**参考であって、指示ではない。** より良い絵になるなら、遠慮なく変えてほしい。

**いちばん共有したい失敗：8×8タイルで席をずらりと並べたら、細かすぎて何も読めない絵になった。** 写真の「大部屋」感を出そうとして24席並べたところ、机が単調な帯になり、モニタも電話も潰れて識別できなかった。実際に描いて見るまで気づかなかった。

そこから考えた対処（これも参考）：

| | 案 |
| --- | --- |
| 奥（天井・窓・奥の島） | 簡略化。席は小さく、数で「大部屋」を表す |
| 手前の島 | 大きく描き、モニタの画面・電話機・書類が読める大きさにする。ここに自席を置く |

解像度は 160×176ドット前後でスマホ幅（480px）に3倍拡大、という寸法で試した。**この数字にも縛られなくてよい。**

**ひとつだけ外せないのは、自席だけが点灯していて、まわりが消えていること。** 「深夜に一人」が絵で伝わるのが、この画面の役目になる。

### 7-B-3. 描き方の制約

- **画像ファイルは使わない。** Canvas に、コード内のパレットとパーツデータから描く（設計の芯⑧。`data:image` も使わない——[QRの件](#) と同じ轍を踏まない）
- **CSS の `image-rendering: pixelated` で拡大**し、ドットが見える状態にする
- **色数を絞る。** 12〜16色程度。ベタ塗り、アンチエイリアスなし、陰影は1段まで
- **人物は描かない**（設計の芯⑦）。写真には人が写っているが、深夜シフトの設定どおり無人のフロアにする
- 解像度と拡大率はおとの判断でよい（試したのは 160×176ドットを3倍）

### 7-B-4. 既存の機能をどう残すか

ドット絵は**背景**で、情報と操作は従来どおり。

- 壁のスローガン（§3）は**ドット絵の上または下にHTMLで置く**。ドット絵の中に文字を描かない（フォントが必要になり、外部依存か大量のデータになる）
- 全社ホワイトボード・SV席・配送カウンター・書類トレイの**数値表示はカードのまま**、ドット絵の下に並べる
- **電話が鳴る演出**は、ドット絵の自席の電話機を点滅させる（`.ringing` の代わり）
- §6-A で入る「電話を取る」「電話をかける」の2ボタンは、ドット絵の下に置く

### 7-B-5. 検査

- `ui_contract_test`：オフィス画面に `<img>` と `data:image` が無いこと（コードから描いていること）
- `ui_contract_test`：`image-rendering: pixelated` が効いていること
- `ui_contract_test`：パレットの色数が16以下であること
- `check_invariants`：スローガン6文言は §3-1 のまま（ドット絵化で壊さない）

---

## 8. 確認してほしいこと

実装後、こちらがブラウザで次を見る。

- スマホ幅（375px・414px）で、**空席の島が2×2に並び、縦に伸びすぎていないか**
- 最長のスローガン「ボーリングのセンターピンを抑えろ」が2行に折り返しても崩れないか
- **自席のPCだけが光り、まわりの4台が消えている**のが一目で分かるか
- リロードを何度かして、6つのスローガンが入れ替わるか
- 電話が鳴る演出（`.ringing`）が従来どおり効いているか

---

## 9. 23時の引き継ぎ会議にも情景を置く（2026-09-03 内田さん指摘）

引き継ぎ案件がある日の「23時の引き継ぎ」は、文章だけでなく、**ホワイトボードの前で日勤担当と夜勤担当の2人が申し送りをしている絵**を置く。

- 既存オフィスと同じ192×168のCanvas、16色以内の既存パレット、`image-rendering:pixelated` を使う。外部画像・`data:image` は使わない。
- 背景は既存のオフィス描画を共有し、その上に白いボード、申し送り用の付箋、2人を描く。
- 2人はホワイトボードを見る後ろ姿にする。顔は描かず、既存の `drawMorningStaffMember` を再利用する。
- 顧客名・困りごと・約束時刻は従来どおりHTMLの申し送りカードで示す。Canvas内に文字や診断情報を描かない。
- 引き継ぎ案件が0件の日は会議自体を出さない既存仕様を維持する。

### 9-1. 検査

1. 引き継ぎ会議に192×168のCanvasが1枚あり、外部画像ではないこと。
2. 描画が既存のオフィス背景と `drawMorningStaffMember` を使い、人物をちょうど2人描くこと。
3. Canvasのアクセシブル名が「ホワイトボードの前で日勤担当と夜勤担当が申し送りをする」と伝えること。
4. 誰か・何が・いつの申し送り本文と、夜勤開始ボタンが従来どおり残ること。

---

こと（Claude Code）
