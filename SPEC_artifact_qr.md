# iPhone で開くためのQRコード 仕様

2026-09-01、内田さんの指示。

> アーティファクトにもiPhone用のQRコードをつけて欲しい

**前に消したQRとは別物。** あちらは開発機のローカルIP（`http://192.168.0.113:8765/`）を指していて、PCでサーバーを立てていないと開けなかった。**今回は公開中の Artifact のURLを指すので、サーバーも同じWi-Fiも要らない。**

---

## 1. 何を指すQRか

```
https://claude.ai/code/artifact/7589f548-a1fe-4416-b101-4194e009e379
```

68文字。**このURLは `HANDOVER.md` 1章の公開先と同じもので、変わったらQRも作り直しが要る。**

---

## 2. 前回の事故を繰り返さないための決めごと

**QRは人の目で中身を検証できない。** 前回それで、内田さんのローカルIPが公開物に埋まっていることに誰も気づけなかった（こちらは公開直前に全行読んでいたのに見逃した）。同じことを防ぐため、次の3つを必ず守る。

1. **`data:image` を使わない。** 白黒の並びを `'0'`/`'1'` の行文字列としてコードに持ち、Canvas で描く。**コードを読めばパターンが見える状態にする**
2. **QRのすぐ下に、URLを平文で表示する。** 読み取れなくても、何を指しているかが目で分かる。手で打つこともできる
3. **検査で、平文のURLが `HANDOVER.md` の公開先と一致することを見る**

---

## 3. パターンデータ

**バージョン5・誤り訂正M・37×37モジュール。** `segno` で生成した。生成手順は §6。

```js
/* https://claude.ai/code/artifact/7589f548-a1fe-4416-b101-4194e009e379 を指すQR。
   1=黒、0=白。周囲に4モジュールの余白（クワイエットゾーン）を必ず取ること。 */
const ARTIFACT_QR = [
  '1111111001110100001011111010001111111',
  '1000001001111001000100000001101000001',
  '1011101010001000000111010011001011101',
  '1011101010111001011110100111001011101',
  '1011101010001000100100110100101011101',
  '1000001011110000101011000001001000001',
  '1111111010101010101010101010101111111',
  '0000000011100000100111101010100000000',
  '1011111001101011001011000100101111100',
  '0000110011111101110001011110000100010',
  '1010001101111010001011100001001100011',
  '1111010010110100101001011011110111001',
  '0101101111111010011010101101011011111',
  '1101000100011100110101010000000100000',
  '1111011010101001110011000011101111011',
  '0001100010000011111111010001100110000',
  '0110001000101100100101010111101010111',
  '1100000001101001011010011100010000110',
  '0001101100111001101101000111110010011',
  '0101010010011011000111100011101101001',
  '0101011101111001010100101110011010100',
  '0110000000110010001100011000100001000',
  '0100001010100101001000100011001111011',
  '0010010001111011000010000011111110011',
  '0100001110111110001011101101001110101',
  '1000010101100011100001010010010000000',
  '1010001111001100010010100101101101011',
  '1011100010010000101001001011011001001',
  '1010111100000000111010111111111110111',
  '0000000011000010010111010001100011010',
  '1111111001111111010011001000101010101',
  '1000001011110001111101100000100011011',
  '1011101010110011100001000101111110101',
  '1011101011111001001011111110011010001',
  '1011101010000101100101001110010001011',
  '1000001001101111000111101010101011001',
  '1111111010101011111100101111001010111',
];
```

---

## 4. 見せ方

**置き場所はブリーフィング画面**（「シフトを始める」の前）。最初に目に入る場所で、前に消したQRがあったのと同じ位置。

```
┌─────────────────────────────┐
│ ▓▓▓  iPhoneで遊ぶ             │
│ ▓▓▓  カメラでQRを読み取ると、  │
│ ▓▓▓  このページが開きます      │
│      https://claude.ai/code/… │  ← 平文で併記
└─────────────────────────────┘
```

- **描画は Canvas。** 1モジュールを整数ピクセルにして `image-rendering: pixelated` で拡大する（にじむと読めない）
- **黒は純黒（`#000`）、白は純白（`#fff`）。** 読み取り精度のため、テーマの色トークンを使わない
- **周囲に4モジュールぶんの白い余白**を必ず取る。ここを詰めると読めなくなる
- **スマホ幅（480px以下）では隠す。** 既にスマホで見ているならQRは要らない。前のQRと同じ扱い
- URLの平文は小さくてよいが、**省略記号で切らない**（手で打てなくなる）

---

## 5. 検査

- `check_invariants`：`ARTIFACT_QR` が37行あり、各行が37文字で、`0`/`1` 以外を含まないこと
- `ui_contract_test`：ブリーフィングに平文のURLがあり、それが `HANDOVER.md` 1章に書かれた公開先と**文字列として一致する**こと
- `ui_contract_test`：QRの描画に `data:image` と `<img>` を使っていないこと（既存の契約で担保されているが、明示的に残す）
- `ui_contract_test`：クワイエットゾーンを確保する処理があること

**negative test：** URLを1文字変える／QRを36行に削る／`data:image` に置き換える／余白の確保を外す。いずれも赤くなること。

---

## 6. 作り直しが必要になったら

**Artifact のURLが変わったら、QRも作り直す。** 手順はこれだけ。

```
pip install --target <作業用ディレクトリ> segno
python3 -c "import segno; qr=segno.make('<新しいURL>', error='m'); print('\n'.join(''.join('1' if c else '0' for c in r) for r in qr.matrix))"
```

出力をそのまま `ARTIFACT_QR` に貼る。**`segno` は生成のときだけ使う道具で、公開物には入らない**（設計の芯⑧に反しない）。

---

## 7. こちらで確かめられないこと

**実機での読み取りは、内田さんにお願いする。** 生成ライブラリの出力を信頼してパターンを起こしているが、**こちらにカメラがないので「本当に読める」ことは確認できていない。** 実装が入ったら、内田さんに iPhone のカメラで試してもらう。

読めなかった場合の手当ては、URLを平文で併記してあること。**QRが機能しなくても、そこから開ける。**

---

こと（Claude Code）
