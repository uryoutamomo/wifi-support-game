# iPhone で開くためのQRコード 仕様

2026-09-01、内田さんの指示。

> アーティファクトにもiPhone用のQRコードをつけて欲しい

**前に消したQRとは別物。** あちらは開発機のローカルIP（`http://192.168.0.113:8765/`）を指していて、PCでサーバーを立てていないと開けなかった。**今回は公開中のGitHub PagesのURLを指すので、サーバーも同じWi-Fiも要らない。**

---

## 1. 何を指すQRか

```
https://uryoutamomo.github.io/wifi-support-game/
```

48文字。**このURLは `HANDOVER.md` 1章の公開先と同じもので、変わったらQRも作り直しが要る。**

---

## 2. 前回の事故を繰り返さないための決めごと

**QRは人の目で中身を検証できない。** 前回それで、内田さんのローカルIPが公開物に埋まっていることに誰も気づけなかった（こちらは公開直前に全行読んでいたのに見逃した）。同じことを防ぐため、次の3つを必ず守る。

1. **`data:image` を使わない。** 白黒の並びを `'0'`/`'1'` の行文字列としてコードに持ち、Canvas で描く。**コードを読めばパターンが見える状態にする**
2. **QRのすぐ下に、URLを平文で表示する。** 読み取れなくても、何を指しているかが目で分かる。手で打つこともできる
3. **検査で、平文のURLが `HANDOVER.md` の公開先と一致することを見る**

---

## 3. パターンデータ

**バージョン4・誤り訂正M・33×33モジュール。** `segno` で生成した。生成手順は §6。

```js
/* https://uryoutamomo.github.io/wifi-support-game/ を指すQR。
   1=黒、0=白。周囲に4モジュールの余白（クワイエットゾーン）を必ず取ること。 */
const ARTIFACT_QR = [
  '111111100011000000111111001111111',
  '100000100111100110101100001000001',
  '101110101101001010011110001011101',
  '101110101000100000100010101011101',
  '101110101000111111001101001011101',
  '100000101101111000110110001000001',
  '111111101010101010101010101111111',
  '000000001001001010011101000000000',
  '101111100110011001010010001111100',
  '011011001000111011011001001101101',
  '010101111000111111000110000010110',
  '110001001011110101111111000011101',
  '101001110011001010011011110011010',
  '111010000010000100100101001000011',
  '111111100011001110001100011101110',
  '001110001100010100100110011100100',
  '000010101101000101000011110110001',
  '000110001001000010110101011101101',
  '110100100110110100101110011110110',
  '011000000010001010011110111111110',
  '001110100101100000110001110011001',
  '110001010011100111001001111001001',
  '100001100010111000110110110001110',
  '101010000101101010111110010001100',
  '101011101100101001010010111111000',
  '000000001000110001011001100010101',
  '111111100111100101001011101010110',
  '100000101011001001101111100011111',
  '101110101110000110010100111111010',
  '101110101000010100100000010010111',
  '101110101000011110000010111101000',
  '100000100101100100000111000011100',
  '111111101011010101000011101101010',
];
```

---

## 4. 見せ方

**置き場所はブリーフィング画面**（「シフトを始める」の前）。最初に目に入る場所で、前に消したQRがあったのと同じ位置。

```
┌─────────────────────────────┐
│ ▓▓▓  iPhoneで遊ぶ             │
│ ▓▓▓  カメラでQRを読み取ると、  │
│ ▓▓▓  このページが開きます      │
│      https://uryoutamomo…     │  ← 平文で併記
└─────────────────────────────┘
```

- **描画は Canvas。** 1モジュールを整数ピクセルにして `image-rendering: pixelated` で拡大する（にじむと読めない）
- **黒は純黒（`#000`）、白は純白（`#fff`）。** 読み取り精度のため、テーマの色トークンを使わない
- **周囲に4モジュールぶんの白い余白**を必ず取る。ここを詰めると読めなくなる
- **スマホ幅（480px以下）では隠す。** 既にスマホで見ているならQRは要らない。前のQRと同じ扱い
- URLの平文は小さくてよいが、**省略記号で切らない**（手で打てなくなる）

---

## 5. 検査

- `check_invariants`：`ARTIFACT_QR` が33行あり、各行が33文字で、`0`/`1` 以外を含まないこと
- `ui_contract_test`：ブリーフィングに平文のURLがあり、それが `HANDOVER.md` 1章に書かれた公開先と**文字列として一致する**こと
- `ui_contract_test`：QRの描画に `data:image` と `<img>` を使っていないこと（既存の契約で担保されているが、明示的に残す）
- `ui_contract_test`：クワイエットゾーンを確保する処理があること

**negative test：** URLを1文字変える／QRを32行に削る／`data:image` に置き換える／余白の確保を外す。いずれも赤くなること。

---

## 6. 作り直しが必要になったら

**公開ページのURLが変わったら、QRも作り直す。** 手順はこれだけ。

```
pip install --target <作業用ディレクトリ> segno
python3 -c "import segno; qr=segno.make('<新しいURL>', error='m'); print('\n'.join(''.join('1' if c else '0' for c in r) for r in qr.matrix))"
```

出力をそのまま `ARTIFACT_QR` に貼る。**`segno` は生成のときだけ使う道具で、公開物には入らない**（設計の芯⑧に反しない）。

---

## 7. こちらで確かめられないこと

**実機での読み取りは、内田さんにお願いする。** 生成ライブラリの出力を信頼してパターンを起こしているが、**こちらにカメラがないので「本当に読める」ことは確認できていない。** 実装が入ったら、内田さんに iPhone のカメラで試してもらう。

読めなかった場合の手当ては、URLを平文で併記してあること。**QRが機能しなくても、そこから開ける。**

---

こと（Claude Code）
