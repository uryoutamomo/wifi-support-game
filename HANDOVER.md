# 引き継ぎ資料 ／ 深夜のグローバルデスク

2026-08-31、内田さんから「ことのトークンが厳しいので、あとはおととやり取りしたい」との指示があり、**こと（Claude Code）から、おと（Codex）へ主担当を引き継ぎます。**

この資料は、こと抜きで作業を続けるために必要なことを全部書いたものです。おとさんと、必要なら新しい Claude セッションが読みます。

---

## 1. まずここを読んでください（公開の方法）

**成果物の URL：** https://uryoutamomo.github.io/wifi-support-game/

**GitHubリポジトリ：** https://github.com/uryoutamomo/wifi-support-game

2026-09-01に公開基盤をGitHub Pagesへ移行しました。`main` ブランチのルートが公開元で、同じURLをおとだけで更新できます。

**引き継ぎ後の公開手順：**

1. `node build.js` と§3の検査を実行します。
2. 生成された `index.html` と編集ソースをコミットし、`main` へpushします。
3. GitHub Pagesのビルド成功を確認し、公開HTMLのSHA-256がローカル版と一致することを確認します。
4. favicon は `📡🌙` で固定です。変えないでください（タブの見た目が変わって別ページに見えます）。

旧Artifactは移行前の参照用として残していますが、**削除・変更しません。**

---

## 2. ファイル構成とビルド

```
~/wifi-support-game/
  p1_head.html          <title>・フォント・CSS・HTML本体
  p2_data.js            シナリオ13件と各種マスタ（データだけ）
  p3_game.js            ゲーム状態・時間進行・質問・会話・解決処理
  p4_view.js            オフィス・通話・配送・報告画面の描画
  p5_events.js          クリック・キー入力から各処理へのルーティング
  source_manifest.js    編集素材の結合順を管理する正本
  index.html            ↑5つを結合したもの。これを公開する
  preview.html          旧ローカル確認用（現在の index.html は単体で表示可能）
  SETTING.md            世界設定の正本（モデル企業と守るべき線引き）
  SPEC_call_flow_v2.md  仕様の正本。§番号で管理していて、いまは §39 まで
  SPEC_office_scene.md  オフィス画面（ドット絵）の仕様
  SPEC_artifact_qr.md   ブリーフィングに出すQRの仕様
  HANDOVER.md           この資料
  verify.js             データ整合性の検査
  check_invariants.js   仕様どおりかの検査
  ui_contract_test.js   コマンドUIとSIM清掃仕様の回帰検査
  progression_test.js   案件間の進行と全正解ルート到達性の回帰検査
  typing_budget.js      文字送りの所要時間の見積もり
  negative_ui_mutation_test.js  検査自体を壊しテストで検証する
  test_helpers.js       検査どうしで共有する処理
  build.js              5つの編集用ソースから index.html を生成
```

**ビルド：**

```
cd ~/wifi-support-game && node build.js
```

**ローカル確認：**

```
cd ~/wifi-support-game && python3 -m http.server 8731
```

`index.html` は標準HTML文書で、直接ブラウザで表示できます。iPhone Safariの文字自動拡大を固定し、外部フォントに依存しない構成です。

---

## 3. 検査（変更のたびに必ず走らせる）

```
node verify.js            # データ整合性。終了コード0でなければ NG
node check_invariants.js  # 仕様どおりか。終了コード0でなければ NG
node ui_contract_test.js  # コマンド階層とSIM清掃の回帰契約
node progression_test.js  # 早期解決後も次の着信へ進み、全案件を完走できるか
node typing_budget.js     # 文字送りが4秒を超える行がないか
node negative_ui_mutation_test.js  # 検査そのものが機能しているか（壊すと赤くなるか）
```

**`negative_ui_mutation_test.js` は「検査を検査する」ものです。** ソースをわざと壊して、対応する検査が赤くなるかを確かめます。**検査が通ったという事実だけでは、検査が働いている証拠になりません。** 新しい不変条件を足したら、それを壊す変異もここへ足してください。

**`check_invariants.js` は「こと」が依頼を出す前の期待値を書き下ろしたものです。**実装側と独立した基準なので、**この検査を緩めて通すことは絶対にしないでください。**落ちたら実装のほうを直すか、内田さんに確認してください。

見ているのは、13件の `trueCause` / `best` / `bestNoOutage` / `partial` / 伝え方の正解 / 液晶の `panel` データ / 配送条件 / 本人特定・再質問・謝罪の条件、そして下の「設計の芯」です。

---

## 4. 絶対に壊してはいけない設計の芯

これまでの積み重ねで、壊すとゲームが成立しなくなるものです。

**① S5・S6・S7 の液晶は同じ見た目でなければならない**
広域障害の2件（`carrier`）と、機種が国に合っていない1件（`coverage`）は、液晶がどれも `bars:0 / carrier:null / sim:'ok'` です。**液晶だけでは区別できないことが診断の芯**で、複数地点で試したか、エリア照会をしたかで初めて分かれます。「分かりやすくしよう」として差を付けると、このゲームの一番おもしろいところが消えます。区別できるのは S8（`sim:'none'`）だけです。

**土地の名前で覚えないでください。** §38 で名前と土地がシャッフルされるようになったので、**「ニューヨークの2件」のような呼び方はもう通じません。** 案件は `id` と `trueCause` で識別してください。

**S5・S6 は同じ地域の別都市へ割り当てられます**（§38-4-2）。2件目を受けたときに1件目と同じ障害だと気づく、というのがこの2件の山場なので、**別々の国へ散らさないでください。**

**② 手がかりを全部集めたら、原因候補はちょうど1つに絞れる**
`verify.js` が見ています。新しい `fact` を足すときは、真の原因を自分で除外していないか必ず確認してください。

**③ SIM未認識では、電源を切らずにSIM抜き差し・接点清掃が第一選択**
No SIM／SIM未認識の表示を確認したら、SIMを抜き差しし、乾いた柔らかい布で接点を清掃します。このゲームの機種では電源OFFを前提にしません。清掃後も認識しない場合だけ交換エスカレーションへ進みます。危険操作として残すのは、本体初期化／スマホのAPN書き換え／データローミングONの3種です。

**④ 誤診しても必ず挽回できる**
1回目の誤診は再入電、2回目で上長が必ず引き取ります。詰み状態を作らない設計です。

**⑤ 業務報告の候補に正解ラベルを出さない**
提出前に「報告対象／日常対応」を見せると、選ぶ意味がなくなります。答え合わせは提出後の振り返りで行います。

**⑥ 実在ブランドを画面に出さない**
モデルは実在サービスですが（`SETTING.md` 参照）、社名・ロゴ・電話番号・ドメインは一切出しません。架空の GLOBAL DESK と TRANSGLOBE EXPRESS(TGX) を使います。

**⑦ 人物・キャラクターを描かない。ただしエンディングだけは例外**
内田さんの決めごとです。オフィス画面も什器と灯りだけで構成しています。**2026-09-01 に内田さんの指示でエンディングだけ例外になりました**（社長と社員10人の後ろ姿）。**通常の業務画面へ人物を持ち込まないでください。** エンディングは「その日が終わった後の景色」なので別扱いです。

**⑧ 外部依存を増やさない**
Google Fonts のみ。CDN のライブラリは不可。単一 HTML で成立させます。

---

## 5. 未実装の指示（次にやること）

### 5-0. 2026-09-01 に §5 の宿題はほぼ片づきました

**この節はもともと 2026-08-31 の指示（リアルタイム化・国際通話料・折り返し）を書き残したものでした。** そのうち通話料と折り返しは、2026-09-01 の内田さんの追加指示（`SPEC_call_flow_v2.md` §39）で**現実に合わせた形で実装済みです。**

**ただし当時の設計とは向きが変わりました。理由も含めて書き残します。**

```
旧5-2 通話料      会社の費用として計上する
   → 現在        客が自分のスマホで国際電話をかけているので、客が払う。
                 折り返した分だけ会社の費用になる（§39-1）

旧5-3 折り返し    ペナルティを軽くして「原則」にする
   → 現在        いったん §24-3 で廃止した。5分の調べ物に30分の折り返しが
                 釣り合わなかったため。§39-7 で「客の通話料を止める」という
                 別の理由をつけて復活させた

旧5-4 折り返し先  携帯／ホテル客室／現地番号から選ばせる
   → 現在        ホテルへ折り返す一本。まずフロントが英語で出る（§39-5）。
                 現地が22時以降だとフロントが渋り、
                 「折り返しである」と伝えると通してもらえる（§39-6）
```

**旧 5-1（リアルタイムの沈黙圧）だけは、いまも未実装です。** 下の 5-1 をそのまま残してあります。**内田さんはまだこれを求めていません。** 実装するなら、書いてあるとおり既存の `stress` への入力として足してください。新しいゲージを作らないこと。

### 5-1. リアルタイム化（FFのアクティブタイムバトル方式）— **未実装**

通話フロー v2 で、行動内容に応じて増減する `stress`（通話ヘッダの「苛立ち」）を実装しました。**この `stress` を顧客の苛立ちを表す唯一の指標とします。** 待ち行列の `patience` は、電話を取るまでの忍耐だけを表す別の値です。

リアルタイムの沈黙圧は、現時点ではまだ実装しません。将来追加する場合も新しいゲージは作らず、既存の `stress` への入力を一つ増やします。

**核心：「顧客が話している時間」と「こちらが黙っている時間」を分ける。**

```
顧客の発話が一文字ずつ出ている間   → 顧客が話している。イライラは溜まらない
発話が出終わってから、プレイヤーが
次のコマンドを選ぶまでの実時間     → 沈黙。ここでイライラが溜まる
```

これがこの設計の勘所です。**読むのが遅い人が罰を受ける形にしてはいけません。** 実際の電話でも、相手が話している間は待たされた感じがしません。オペレーターが黙り込んでいる時間だけがストレスです。一文字ずつ表示（ポートピア方式）と、スピードを求める設計は、この切り分けで両立します。

- 沈黙が続くと、通話中の顧客の `stress` が実時間で増えていく
- 目安：沈黙3秒までは増えない（考える時間）。それ以降 **1秒あたり +1.5 × `stressRate`**
- 全文送り（クリック／Space／Enter）で表示を飛ばした場合、その時点から沈黙の計測を始める
- **`prefers-reduced-motion` のときは沈黙の圧も止める**（アクセシビリティ上、時間で罰する設計は無効化する）

行動してもストレスが増え、考えていても別ゲージで罰する二重構造にはしません。**実装してみて「理不尽だ」と感じたら、内田さんに戻して調整を相談してください。** 沈黙の許容時間（3秒）と増加率は机上の数字で、実際に遊んで確かめていません。厳しすぎるとテキストを読む余裕がなくなり、これまで積み上げた診断のおもしろさが台無しになります。**演出のためにゲームの中身を壊さないこと**が、この案件でずっと守ってきた線です。

### 5-2. いま残っている宿題（2026-09-02 時点）

**内田さんが遊んで判断する番のものが多いです。数値は勝手に触らないでください。**

1. **症状とタイプのシャッフル（第2段階）** — 名前と土地は §38 で動くようになりました。**症状まで動かす意味を内田さんへ確認中で、回答待ちです。** 台詞の全面的な作り直しになるので、着手前に必ず仕様を確定させてください
2. **裏エンディングの中身** — バッジ8種の制覇で出ますが、暫定的に表と同じ絵を出しています（§28）
3. **§12（難易度・案件ごとの業務報告・評価演出）** — 仕様だけあって未実装
4. **診断ボードのテストモード化** — §20 で位置づけだけ決め、実装は未着手
5. **苦情メールの重なりと引用の長さ** — 同じタイプの客が複数怒ると本文が似る。引用（第一声の全文）もやや長い

### 5-3. 参考：2026-08-31 時点の折り返し先の設計（不採用）

**当時、折り返し先を3つから選ばせる設計を書いていました。** §39 でホテル一本になったので採りませんでしたが、**将来「移動中の客」を作るときに考え方が使えます。**

```
① お客様の携帯（日本の番号・ローミング中）
   → 確実に本人に繋がるが、着信側にも国際ローミングの通話料がかかる
② 滞在先のホテル客室
   → 客に費用がかからない。日本人の個人客ではこれが原則
③ 現地で契約した番号 → 持っている客だけ。該当なし
```

**重要な制約：客はインターネットが使えないから電話しています。** LINE や WhatsApp での折り返しは成立しません。**この選択肢を作らないでください**（作ると業務として嘘になります）。この制約はいまも生きています。

---

## 6. これまでの経緯（判断の背景）

短く振り返ります。同じ議論を繰り返さないためのメモです。

1. **最初はメール対応の作りだった。** 同時に3件を並行処理し、再起動を待つ間に別の客を捌く設計でしたが、内田さんから「これはメール対応では」と指摘され、**単一通話・保留・折り返し**の電話対応へ全面的に作り替えました。並行処理は受信箱の発想でした。
2. **ダークテーマは失敗だった。** 「深夜シフトだから暗い画面」は演出の理屈で、業務画面の理屈ではありませんでした。10px以下の文字が12箇所あり、読みづらいと評価されました。いまはライト単一です。**同じ理由で暗い方向へ戻さないでください。**
3. **おとさんのレビューで芯が決まった。** 「原因を一発で当てるクイズ」になりかけたのを、**確信度3段階（確定／有力／要エスカレーション）**という切り分けに変えたのがおとさんの提案です。これがゲームの背骨です。
4. **検査を2本持っているのは事故が起きたから。** 「集めた手がかりが真の原因を自分で除外していて、診断ボードが嘘をつく」状態が実際にありました。機械で見るようにしてから、この種の事故は止まっています。

---

## 7. 内田さんへの連絡について

**2026-09-01 に、内田さんが実際に何度も遊んで指示を出しました。** その日に出た不具合4件（機器を受け取っていない客に機器の話をする／SIM清掃で直ったのに終われない／終われない理由を言わない／S12の流れが不自然）は、**どれも検査が全部通っていて、データも仕様どおりでした。**

**共通していたのは「画面に出ていない」ことです。** `recentTranscriptLines` は `who` が `me` と `cust` の行しか通話画面に出しません。`note` / `sys` / `front` に入れた文は、会話記録を開かないと見えません。**大事なことを `note` に書いて安心する、という間違いを2度やりました**（§36 と §39）。

**遊んで分かることは、検査では見つかりません。** 内田さんへ渡す前に、自分でひととおり通してください。

放棄呼の閾値（`abandonAfter`）、エスカレーション枠、通話料の単価あたりは、まだ遊んだ感触での調整が要ると思います。**評価の配点については内田さんが「そもそも評価点は気にしていなかった」と言っているので、配点を合わせるために現実のほうを曲げないでください。**

こと（Claude Code）
